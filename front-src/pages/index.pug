doctype html
html(lang='ru')
	head
		meta(charset='UTF-8')
		meta(name='viewport', content='width=device-width, initial-scale=1.0')

		<!-- Yandex.RTB -->
		script window.yaContextCb=window.yaContextCb||[]
		script(src='https://yandex.ru/ads/system/context.js', async)

		title Расписание занятий. ДВГГТК
		link(rel='stylesheet', href='css/style.css')
		style.
			.control {
				padding: 0.3em;
			}

	body 
		header.header(style='position: sticky')
			form(name='filter', style='display: flex; flex-wrap: wrap') 
				.control 
					input#date(type='date', name='date')
				.control
					select#group(name='group')
						option(value='') <группа>
				.control
					select#teacher(name='teacher')
						option(value='') <преподаватель>
				.control
					select#room(name='room')
						option(value='') <аудитория>
				.control
					select#oneDay(name='oneDay')
						option(value='') <один день>
						option(value='groups') Группы
						option(value='teachers') Преподаватели
						option(value='rooms') Аудитории
		main#timetable-container.main(style='position: relative')
			section#timetable(
				style='overflow-y: auto; position: absolute; top: 0; left: 0; right: 0; bottom: 0; width: 100%; max-width: 100%'
			)

		footer.footer

		script(src='js/index.js') 

		<!-- Yandex.Metrika counter -->
		script.
			(function (m, e, t, r, i, k, a) {
				m[i] =
					m[i] ||
					function () {
						(m[i].a = m[i].a || []).push(arguments);
					};
				m[i].l = 1 * new Date();
				for (var j = 0; j < document.scripts.length; j++) {
					if (document.scripts[j].src === r) {
						return;
					}
				}
				(k = e.createElement(t)), (a = e.getElementsByTagName(t)[0]), (k.async = 1), (k.src = r), a.parentNode.insertBefore(k, a);
			})(window, document, 'script', 'https://mc.yandex.ru/metrika/tag.js', 'ym');

			ym(90878793, 'init', {
				clickmap: true,
				trackLinks: true,
				accurateTrackBounce: true,
				webvisor: true,
			});
		noscript
			div: img(
				src='https://mc.yandex.ru/watch/90878793',
				style='position: absolute; left: -9999px',
				alt=''
			)
		<!-- /Yandex.Metrika counter -->
